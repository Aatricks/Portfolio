<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
		justify-content: center;
		align-items: center;
		padding: 0.25rem 0.75rem;
		gap: 0.35rem;
		color: var(--gray-300);
		background-color: transparent;
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 4px; /* Restrained rounded corners */
		font-family: var(--font-brand);
		font-size: 0.8rem;
		line-height: 1.35;
		white-space: nowrap;
	}
</style>