<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
        justify-content: center;
        align-items: center;
		padding: 0.3rem 0.55rem;
		gap: 0.35rem;
		color: var(--accent-text-over);
		background-color: color-mix(in srgb, var(--accent-regular) 80%, transparent);
		border-radius: 999rem;
		font-size: 0.88rem;
		line-height: 1.35;
		white-space: nowrap;
	}
</style>
